package review.controlflow;

public class LearnIfElse {

    //if condition){}
    //if condition is true then it will execute if block and
    //if condition is false it will execute else block
    //ctrl+alt +L / command +alt /option+L = format code
    public static void main(String[] args) {
        //if (condition){statement}
        int age = 15;

        if (age > 18) {
            System.out.println( "you are eligible for vote" );
        } else {
            System.out.println( "you are not elegible for vote" );


        }
    }
}